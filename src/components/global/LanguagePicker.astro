---
export const languagesPicker = {
  es: {
    label: "Español",
    value: "es",
    image: "/assets/es.png",
  },
  en: {
    label: "English",
    value: "en",
    image: "/assets/en.png",
  },
};
---

<ul
  class="bg-black fixed z-50 bottom-2 right-2 flex gap-x-4 px-2 py-2 rounded-full w-fit"
>
  {
    Object.entries(languagesPicker).map(([lang, label]) => (
      <li>
        <a href={`/${lang}/`}>
          <img
            src={label.image}
            alt={label.value}
            class="w-8 h-8 hover:transform hover:scale-110 duration-300"
          />
        </a>
      </li>
    ))
  }
</ul>